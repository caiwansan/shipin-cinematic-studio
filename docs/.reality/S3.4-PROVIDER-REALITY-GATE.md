# S3.4-PROVIDER-REALITY-GATE.md

> S3.4.1.5 Provider Reality Gate — 真实 Provider 受控调用验证
> 日期: 2026-08-06 07:30 (CST) | 状态: ⏳ **PR1/PR4 待 DEEPSEEK_DEV_API_KEY 点火**（PR2/PR3/PR5 已 PASS）
> 提交: feat(ai): development provider channel (S3.4.1.5)
> 依据: 掌柜 S3.4.1.5 执行指令（只实施 Provider 验证, 不进入 AI Employee）

---

## 0. 阶段边界

```
✅ 验证: 真实 LLM Provider 经 Unified AI Gateway 完成受控调用
⛔ 不做: Planner LLM / Skill LLM Tool / AI Employee Reality（S3.4.2+）
禁止: 绕过 Gateway / 新增调用路径 / 修改 BYOK 语义 / 接入业务
```

## 1. Task 01 — Provider 配置审计（结论）

| 资产 | 现状 |
|---|---|
| Provider Registry | ✅ gateway 内置 adapters: aliyun / volcengine / deepseek / openai（provider-registry.service.ts 为另一数据流, DB 表 provider_registry 存在） |
| Gateway Invoke | ✅ invokeAI: guard(速率/并发) → resolver → adapter → 超时(30s)+重试(2) → InvocationLog |
| Key Resolver | ✅ userModelResolver（BYOK V2 优先）; **S3.4.1.5 新增 dev 兜底（仅解析层, 调用路径不变）** |
| Invocation Log | ✅ **表存在**（`"InvocationLog"`, 实测 to_regclass 确认）; 字段 provider/model/status/latencyMs/errorMsg |
| Cost Tracking | ✅ api-router.service 成本表: deepseek 0.001 / volcengine 0.0008 / aliyun 0.002（llm） |

## 2. Reality 更正（S3.4-LLM-PROVIDER-REALITY-AUDIT.md 修订）

此前侦察误报「UserModelConfigV2 / InvocationLog 表未建」——**结论错误**：
- `"UserModelConfigV2"` 表存在（PascalCase; 此前查询用小写引号导致 not found）
- `"InvocationLog"` 表存在（此前 pg_tables 搜索被 head 截断误判）

**修正后的两层阻断**:
```
L1（仍成立）: 环境无可用 LLM Key（DEEPSEEK_API_KEY 未配 / OPENAI 占位 / 无 ollama）
L2（撤销）:   BYOK 机制完好（表在, resolver 正常）——不再构成阻断
```

## 3. Task 02 — Development Provider（掌柜阶段 A 方案）

```
user-model-resolver.ts 增加 dev 兜底（resolve 返回 null 时）:
  条件: KUNLUN_DEV_PROVIDER=1 且 DEEPSEEK_DEV_API_KEY 存在
  返回: provider=deepseek / model=DEEPSEEK_LLM_MODEL / key=dev key / baseUrl=DEEPSEEK_BASE_URL
顺序: BYOK 用户配置优先 → dev 兜底（不覆盖用户配置）
约束: 仅 dev/test / 不进入用户模型配置 / 不参与计费 / 不进入 Marketplace / 零新表
```

## 4. Task 03 — 调用测试（PR1-PR5）

| # | 关卡 | 状态 | 证据 |
|---|---|---|---|
| PR1 | 真实 API 成功 | ⏸ PENDING | 等待 DEEPSEEK_DEV_API_KEY（.env 追加即点火） |
| PR2 | Gateway only | ✅ | 测试仅 import unified-ai-gateway; 无 provider SDK 直连 |
| PR3 | 错误处理正确 | ✅ | 无 Key → failEnvelope「未配置模型」（优雅失败, 非崩溃） |
| PR4 | Audit 完整 | ⏸ PENDING | 随 PR1 点火（InvocationLog 表已确认存在） |
| PR5 | Secret Isolation | ✅ | .env gitignored / 测试不输出 key / InvocationLog 无 key 字段 |

**点火条件**: 服务器 `backend/.env` 追加 `DEEPSEEK_DEV_API_KEY=sk-…` + 重启 api-server → PR1/PR4 自动全跑。

## 5. Key 安全纪律

```
Key 不进 git/文档/chat/日志 → .env（gitignored）唯一存放 → mask 验证
dev key 与商业隔离: 不入用户配置、不计费、不进 Marketplace（掌柜阶段 A 冻结）
```

## 6. 完成后暂停

```
⏳ 等待掌柜提供 DEEPSEEK_DEV_API_KEY → PR1/PR4 点火
→ 掌柜批准 → S3.4.2 Planner Intelligence（意图→SkillPlan, invokeAI）
```

## 7. 结论

```
S3.4.1.5 Provider Reality Gate: 通道就绪（dev 兜底, 调用路径唯一经 Gateway）
企业级行为保持: 无模型 = 不能调用 AI（未引入偷偷使用平台 Key 的路径）
```
