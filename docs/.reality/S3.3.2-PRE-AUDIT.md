# S3.3.2-PRE-AUDIT.md

> S3.3.2 Skill Composition Enhancement — Task 01 现状审计（只读）
> 日期: 2026-08-06 06:20 (CST) | 状态: ✅ 三项宪法点确认
> 依据: S3.3.2 掌柜执行指令 Task 01 / 《昆仑镜工程开发宪法》

---

## 0. 审计对象

| 资产 | 位置 | 状态 |
|---|---|---|
| skill-orchestrator.ts | backend/src/ecosystem（Cloud 服务层） | ✅ Planner |
| SkillPlan 状态机 | 同上（PlanStatus/StepStatus 类型） | ✅ 掌柜冻结语义 |
| Hermes Invocation Contract | tools/hermes-runtime-skill.mjs（127.0.0.1:9457） | ✅ 原子执行者 |
| KernelEvent Audit | backend/src/routes/hermes-audit.routes.ts | ✅ Cloud Authority |

## 1. 宪法点确认（必须为真）

### A. Planner 仍在 Cloud ✅
- skill-orchestrator.ts 位于 `backend/src/ecosystem`（云服务层）
- hermes runtime 中 orchestrator 引用 = **0**

### B. Hermes 无编排逻辑 ✅
- hermes runtime 中 `dependsOn/topoSort/skillSet/fallback` 编排关键字 = **0**
- Hermes 只做: 收 Invocation → Tool Policy → Sub-Agent → 单工具执行 → Audit

### C. SkillPlan 不入库 ✅
- orchestrator 中 prisma 引用仅 1 处（动态 import + `agentDefinition.findUnique` **只读**，F1 能力读取）
- 零 `create/update/delete`；无 skillPlan 表；无 migration

## 2. 当前状态机（冻结语义，S3.3.2 不改变）

```
Plan:  CREATED → PLANNING → RUNNING → COMPLETED | PARTIAL_COMPLETED | FAILED | CANCELLED（预留）
Step:  PENDING → RUNNING → COMPLETED | FAILED | SKIPPED
Fallback: STOP | SKIP | CONTINUE
```

## 3. 当前 Hermes Invocation Contract

```
POST /invocations
请求: { invocationId, skillId, agentDefinitionId?, tool, input, policy: { allowedTools, deniedTools } }
响应: { status: COMPLETED|POLICY_REJECTED, executionId, runtimeId, agentId, subAgentStates, toolCalls, result }
```

## 4. 当前 Audit 形态（KernelEvent, 无独立表）

```
plan 级:   executionId=planId, runtimeId=skill-orchestrator, status, toolCalls(steps 摘要), result{skillSet,fallback}
step 级:   executionId=exec-*, runtimeId=hermes-skill-001, toolCalls[{tool,allowed}], result, status
```

## 5. S3.3.2 缺口（Task 02-04 将补齐）

| 缺口 | 现状 | S3.3.2 目标 |
|---|---|---|
| timeout | 固定 10s（AbortSignal.timeout） | 可配置 per-step/plan |
| retry | 无 | maxAttempts + backoff + 错误分类（transient 重试 / deterministic 不重试） |
| execution deadline | 无 | plan 级 deadlineMs → FAILED(DEADLINE_EXCEEDED) |
| step 错误分类 | 仅 error 字符串 | errorType 枚举（TIMEOUT/HERMES_UNREACHABLE/POLICY_REJECTED/…） |
| parallel steps | 顺序执行 | 同层独立 step 并行（level-based） |
| dependency failure | 仅顺序保证 | 依赖失败 → 依赖者 SKIPPED(DEPENDENCY_FAILED) |
| audit 明细 | plan 级摘要 | +duration/failureReason/attempts/errorType/step executionId |

## 6. 红线确认（S3.3.2 不触碰）

- ❌ 不新建 Skill SSOT / 不改 AgentDefinition.capabilities
- ❌ 不改 Hermes 生命周期（Sub-Agent 状态机不动；仅工具注册表新增 2 个测试工具 mock.flaky/mock.slow）
- ❌ 不改 Commerce / Identity / 不接真实 AI / 不引入第二套 Agent Runtime

```
✅ Pre-Audit 完成 — S3.3.2 可进入 Task 02
```
