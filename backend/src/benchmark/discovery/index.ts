/**
 * Discovery Module — 统一导出
 *
 * P0-T005 — AI Discovery Lab MVP
 */

export { DiscoveryReport, DiscoveryScenario, DiscoveryOpportunity } from './types';
export { DiscoveryService, discoveryService } from './discovery-service';
export { MockScanner, mockScanner } from './mock-scanner';
